# ADR 0004: Report 2.0 and Stable Fingerprints

Production contracts use schema 2.0. Finding identity hashes the rule, normalized
path, evidence kind, and normalized evidence/excerpt rather than the display line.
Report 1.0 remains readable, but its fingerprints cannot seed 2.0 baselines.
